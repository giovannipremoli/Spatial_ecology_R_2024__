# Multitemporal analysis of vegetation health and landscape variability in the Blue Mountains National Park (Australia) due to the bushfires of 2019-2020.

# Installing the packages necessary for the analysis. The function is install.packages(). 
install.packages("terra") # Used for handling spatial data, particularly raster and vector data (e.g. satellite images).
install.packages("ggplot2") # Used for creating statistical graphs. 
install.packages("patchwork") # Used for combining multiple plots in a single window for comparisons.
install.packages("viridis") # Used for colorblind-friendly and uniform color palettes

# Installing "imageRy" package from GitHub
install.github("ducciorocchini/imageRy")

